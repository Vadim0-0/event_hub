import type { LocalizedScalar } from '~/types/i18n/localizedPage';

export type LocalizedLoadMoreBtn = {
  loadMore: LocalizedScalar;
  loading: LocalizedScalar;
};

export type MappedLoadMoreBtn = {
  loadMore: string;
  loading: string;
};
